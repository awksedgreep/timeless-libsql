# Query and storage findings

This append-only log records storage behavior discovered while implementing
the [PromQL](PROMQL_FEATURE_MATRIX.md) and
[LogsQL](LOGSQL_FEATURE_MATRIX.md) feature maps. It is not a substitute for a
regression test. Every resolved finding links to the test and the public
documentation that now pins the behavior.

## Baseline constraints

These are known constraints at the 2026-08-04 `a72634e` baseline. They are not
new defects, but query work must not accidentally obscure them.

| ID | behavior | consequence |
|---|---|---|
| `QSF-001` | Metric window kernels use `(T-window,T]`. | This matches PromQL range-vector boundaries, but callers must still implement lookback, staleness, counter extrapolation, and output-label rules. |
| `QSF-002` | `timeless_window(..., 'rate'|'increase')` is a storage kernel, not a complete PromQL evaluator. | The Rust API must apply PromQL reset, extrapolation, carry-in, sparse-series, and name-retention semantics rather than relabeling the native result as PromQL. |
| `QSF-003` | Metric storage contains float samples and timestamps but no native-histogram sample type. | Classic `_bucket` histograms are queryable as float series. Native-histogram-only operators and functions remain deferred until a typed storage design exists. |
| `QSF-004` | Stored metrics do not currently preserve an explicit Prometheus stale-marker contract. | Five-minute lookback can be implemented, but exact stale-marker parity needs a storage/ingest decision and must not be claimed implicitly. |
| `QSF-005` | Logs retain typed nested metadata, while the term index intentionally covers only selected low-cardinality keys. | Arbitrary metadata predicates are correct through bounded decode, but only indexed keys can promise posting-list pruning. Measurements decide whether another key earns indexing. |
| `QSF-006` | Message substring filtering and many metadata transformations require block decode. | The API can implement them correctly now; an extension index or new block metadata is justified only by measured workloads. |
| `QSF-007` | Timeless logs do not currently assign VictoriaLogs-compatible `_stream_id` identities. | Ordinary field filters fit now. `_stream_id`, stream-context, and stream-specific optimizations are deferred pending an explicit stream model. |

## Findings

Add one entry for every unexpected correctness, performance, durability,
memory, or planner behavior found during feature work.

| ID | date | matrix row | observation | expected behavior | evidence/test | disposition | status |
|---|---|---|---|---|---|---|---|
| `QSF-008` | 2026-08-04 | `LQL-F02` | Relative LogsQL time was computed from the process wall clock inside parsing and set no upper bound, so deterministic boundary coverage was impossible and future-dated rows matched. Converting the corrected half-open interval directly also made valid `_time:0s` look like a malformed reversed range. | One request-owned evaluation instant defines upstream's `[now-duration,now)` native-unit window; zero duration is a valid empty bounded query; both reach the same public planner used by the HTTP route. | pinned `LQL-F02-relative-window` and `LQL-F02-zero-duration-is-empty`; `relative_time_uses_the_injected_query_clock_exactly`; `session_ten_relative_logsql_pins_inclusive_lower_exclusive_upper_and_reopens` | Extracted a reusable Rust LogsQL planner, injected one query clock, set `ts_min=now-duration` and the inclusive storage bound to `ts_max=now-1 native unit`, and represent `0s` with impossible inclusive bounds that return no rows without unbounded work. Both microsecond edges and the empty case pass before flush and after reopen. Storage formats, batching, indexes, compression, and extension contracts are unchanged. | resolved |
| `QSF-009` | 2026-08-04 | `LQL-F06` | The exact `error` baseline prunes to one error-family block but decodes 4,096 entries to return 1,024 because `error`, `critical`, `alert`, and `emergency` share a physical partition. | Exact severity fidelity is preserved even when the coarse partition needs decode. | `docs/evidence/2026-08-04_query_baseline.json`; `http_uses_the_established_8192_entry_buffer_without_request_flushes` | Accepted storage/write tradeoff. Consider finer severity metadata only after representative workloads prove a material read benefit without harming 8,192-entry batching/compression. | accepted |
| `QSF-010` | 2026-08-04 | `PQL-S06` | The public raw TVF uses inclusive read bounds, while a PromQL range selector uses `(T-W,T]`. | The API and direct SQL recipe must discard samples exactly on the left boundary without changing the extension contract. | `tests/cli.sh` section 33; `session_four_pins_promql_selector_window_errors_and_reopen` | Keep the general raw contract intact and apply the one-boundary predicate in SQL/Rust. No new extension primitive or format change is justified. | accepted |
| `QSF-011` | 2026-08-04 | `PQL-R01` | The existing Rust `avg_over_time` response retained `__name__`, despite the pinned Prometheus fixture already requiring range-function metric-name removal. | Range-function output labels omit the metric name. | `tests/query_oracles/prometheus/promql_smoke.yml`; `session_four_pins_promql_selector_window_errors_and_reopen` | Corrected both packed-window and raw fallback response paths and pinned the exact label set. | resolved |
| `QSF-012` | 2026-08-04 | `PQL-S06` | Metrics API accounting exposed returned frame/response bytes but the public extension stats had no candidate-chunk, payload-byte, or decode counters for packed raw reads. | Direct SQLite/libSQL users and API benchmarks can attribute selected series, candidate chunks, stored payload bytes, fully decoded points, buffered points considered, and returned points. | `tests/cli.sh` section 34; `docs/evidence/2026-08-04_session2_pql_s06.json` | Added cumulative, per-process `raw_batch_query_*` counters to `timeless_stats`; storage and frame formats are unchanged. | resolved |
| `QSF-013` | 2026-08-04 | `PQL-S06` | An incremental local release build retained the previous Git SHA because the common build script watched only an environment override, not the active Git ref. | Checked evidence must identify the exact source commit that produced every measured signal binary. | `test_evidence_rejects_a_stale_release_binary_identity`; Session 2 evidence harness | The build script now watches `HEAD`, its symbolic ref, and `packed-refs`; the evidence harness fails before workload setup when a binary SHA differs from `HEAD`. Release CI's explicit SHA override remains authoritative. | resolved |
| `QSF-014` | 2026-08-04 | `PQL-S11`, `PQL-S01`, `PQL-R01` | The Rust response formatter appended `.0` to integral float samples, while Prometheus emits the shortest value string (`"10"`, not `"10.0"`). | Scalar, vector, matrix, and range-function values share Prometheus's shortest finite formatting plus exact `NaN`/`+Inf`/`-Inf` spellings. | `tests/query_oracles/prometheus/api_cases.json`; `session_two_promql_scalar_literals_match_prometheus`; `session_four_pins_promql_selector_window_errors_and_reopen` | Corrected the shared formatter and updated every existing exact response regression. Stored float bits and SQLite formats are unchanged. | resolved |
| `QSF-015` | 2026-08-04 | `PQL-S12` | PromQL has millisecond durations and evaluation timestamps, while the retained metric storage and packed window TVF use integer seconds. Passing truncated times into the window TVF would silently move boundaries and grid timestamps. | Preserve the second-native storage contract while evaluating exact millisecond boundaries and timestamps. | `PQL-S12-compound-duration-scalar`; `PQL-S12-subsecond-range-step`; `session_two_promql_duration_literals_preserve_milliseconds`; `docs/evidence/2026-08-04_session2_pql_s12.json` | The API uses a millisecond clock. Whole-second windows retain the packed window path; any subsecond temporal component falls back to the public packed raw path and post-filters exact boundaries. No extension or format change was justified. | resolved |
| `QSF-016` | 2026-08-04 | `PQL-S12`, `PQL-S19` | Invalid Prometheus `time` and `step` inputs silently fell back to defaults, broadening a malformed request into a valid query. | Invalid temporal syntax fails as `bad_data`; omitted parameters alone receive defaults. | `prometheus_time_and_duration_inputs_use_a_millisecond_clock`; `session_two_promql_duration_literals_preserve_milliseconds` | Replaced permissive parsing with explicit errors and the pinned duration parser. Exact upstream error-message parity remains owned by `PQL-S19`; no malformed value executes. | resolved |
| `QSF-017` | 2026-08-04 | `PQL-S16` | Prometheus accepts `lookback_delta=0` and interprets it as the configured five-minute default, not as an empty or exact-timestamp window. | Request-scoped lookback must preserve the upstream zero/default distinction and open-left boundary. | `PQL-S16-open-lookback-boundary`; `PQL-S16-zero-selects-default-lookback`; `session_two_promql_grid_and_lookback_match_prometheus`; `docs/evidence/2026-08-04_session2_pql_s16.json` | Added a pinned real-Prometheus remote-write oracle and apply the resolved lookback to every lowered selector. | resolved |
| `QSF-018` | 2026-08-04 | `PQL-S12`, `PQL-S16`, `PQL-S19` | Passing a negative or non-finite numeric duration into the upstream parser utility can panic while constructing `std::time::Duration`; the API previously called it directly for `step`. | Malformed public input must return `bad_data` and never unwind the server. | `prometheus_time_and_duration_inputs_use_a_millisecond_clock`; metrics API unit suite | Numeric duration validation now rejects negative/non-finite/range-invalid values before calling the pinned grammar parser. | resolved |
| `QSF-019` | 2026-08-04 | `PQL-S16`, `PQL-S20` | The range-grid size check used saturating `i64` subtraction followed by unchecked `+ 1`; extreme valid timestamps could wrap in release builds and bypass the 11,000-point gate. | Grid cardinality calculation is exact over the complete accepted timestamp domain. | `prometheus_grid_size_rejects_extreme_ranges_without_overflow`; metrics API unit suite | Grid size is calculated in `i128` before applying the limit. Intermediate/output limit generalization remains owned by `PQL-S20`. | resolved |
| `QSF-020` | 2026-08-04 | `PQL-S18` | The initial matrix named ordinary SQL as the foundation for Prometheus value types, but SQLite rows cannot honestly represent Prometheus's scalar/string/vector/matrix result envelopes, IEEE value strings, label policy, and evaluation timestamps as one portable equivalent. | SQL support is claimed only where a public parameterized statement preserves the promised semantics. | `session_two_promql_value_types_match_prometheus`; `docs/evidence/2026-08-04_session2_pql_s18.json` | Changed the foundation to `none`; value typing remains API-owned and no misleading SQL recipe is published. Public SQL remains the storage-data foundation of the selector/range rows themselves. | resolved |
| `QSF-021` | 2026-08-04 | `PQL-S19` | The Rust range endpoint defaulted omitted `start`, `end`, and `step`, while Prometheus requires each parameter and reports which one is missing. | Canonical Prometheus range requests fail before planning or storage reads when a required parameter is absent. | pinned `PQL-S19-*` API cases; `session_two_promql_errors_match_the_documented_contract`; `docs/evidence/2026-08-04_session2_pql_s19.json` | Prometheus request parsing now requires all three fields and emits parameter-scoped `bad_data`; native Timeless range parameters retain their separate defaults. | resolved |
| `QSF-022` | 2026-08-04 | `PQL-S19`, `PQL-S20` | Prometheus 3.13.2 ignores unknown query parameters, accepts non-finite `time`, and permits 11,000 intervals (11,001 points). Timeless previously mixed permissive defaults with stricter behavior without stating a tier. | Security/resource behavior that intentionally differs from the oracle is explicit and stable. | pinned Prometheus API fixture; `session_two_promql_errors_match_the_documented_contract`; `prometheus_grid_size_rejects_extreme_ranges_without_overflow` | Keep strict unknown-parameter and finite-time validation plus the stated 11,000-point ceiling. Document these as intentional Timeless compatibility differences; `PQL-S20` completes the remaining intermediate limits. | accepted |
| `QSF-023` | 2026-08-04 | `PQL-S19` | The unprefixed `/api/v1/query` routes multiplex the pre-existing native Timeless API based on the presence of `query`; therefore an omitted `query` cannot have the same error as the canonical Prometheus route without breaking native clients. | Prometheus compatibility has an unambiguous canonical route while retained native behavior stays reachable. | `session_two_promql_errors_match_the_documented_contract`; server route tests | `/prometheus/api/v1/query*` is canonical for exact request parsing. Unprefixed routes share PromQL behavior only when `query` is present; this exception is documented rather than hidden. | accepted |
| `QSF-024` | 2026-08-04 | `PQL-S20` | Final HTTP row/byte limits could reject an oversized result only after `timeless_raw_frame` or `timeless_window_batches` had read, decoded, and materialized every candidate point. | Resource limits bound storage decode and packed-result allocation before work is performed, including for direct SQLite/libSQL callers. | `write_flush_query_recover`; `tests/cli.sh` sections 22 and 34; `session_two_promql_limits_bound_grid_work_results_response_and_deadline` | Added optional, capability-advertised `max_work_points` hidden inputs. Existing arities and storage formats remain compatible; the Rust API requires the advertised guard for PromQL and applies result/response/deadline limits while evaluating. | resolved |
| `QSF-025` | 2026-08-04 | `PQL-S20` | After a Git commit that changed no extension source file, Cargo reused `libtimeless_ext.so` with the previous commit embedded in `timeless_capabilities()`, even though the signal binaries rebuilt with the current identity. | Every measured server and extension artifact identifies the exact checked evidence source. | `test_evidence_rejects_a_stale_release_extension_identity`; query evidence preflight | Made the extension build script watch Git HEAD, its symbolic ref, and packed refs just like the server build; evidence now rejects a stale extension before workload setup. | resolved |
| `QSF-026` | 2026-08-04 | `PQL-S04` | Public packed metric reads intentionally take one exact metric name; a PromQL nameless selector can span many names. | Catalog-prune names and series before payload decode, then compose bounded exact-name reads without teaching the extension PromQL syntax. | `session_three_promql_nameless_selectors_expand_before_reads_and_reopen`; pinned Prometheus smoke cases | Reused `timeless_series` for no-decode expansion and `timeless_raw_frame` for each selected name. Result/work/response limits and cancellation span the Rust composition; no extension primitive or storage-format change is justified. | accepted |
| `QSF-027` | 2026-08-04 | `PQL-S04` | The first correct nameless plan queried `timeless_series` once per metric name, costing 2.883 ms p50 and 3.145 ms p95 to return one series from a 576-series/65-name fixture. | Multi-name planning should cross the public catalog once and remain bounded even when few or no series match. | `docs/evidence/2026-08-04_session3_pql_s04.json` before/final measurements; `session_three_promql_nameless_selectors_expand_before_reads_and_reopen` | Stream the complete public catalog once, check cancellation per row, cap considered series and retained label bytes, group matching rows by metric, then issue exact-name payload reads. No shadow-table access or extension change. | resolved |
| `QSF-028` | 2026-08-04 | `PQL-S07`, `PQL-S08` | `timeless_grid` and `timeless_window*` correctly emit their lookup-grid timestamps, while PromQL `offset`/`@` must emit the outer evaluation timestamp after selecting at a different time. | Never present a shifted native-kernel timestamp as modified PromQL output. | `tests/cli.sh` section 45; `session_three_promql_temporal_modifiers_preserve_selection_and_output_time`; pinned `PQL-S07`/`PQL-S08` API cases | Temporal modifiers use bounded public raw frames and apply exact lookup boundaries while writing outer timestamps in Rust. The executable SQL recipe shifts or cross-joins the outer grid explicitly. No extension change is justified. | accepted |
| `QSF-029` | 2026-08-04 | `PQL-S09` | A root subquery can reuse the bounded streaming matrix response, but a parent range function needs random/rolling access to those inner points; globally refactoring selectors into an owned value graph would add that memory cost to every query. | Bound and expose only the intermediate materialization required by a parent AST node while preserving streaming selector hot paths. | `session_three_promql_subqueries_align_bound_cancel_and_reopen`; `session_four_cancels_dropped_promql_requests_and_reuses_the_reader`; `docs/evidence/2026-08-04_session3_pql_s09.json` | Keep the general extension unchanged. The Rust API recursively reuses public packed reads, caps cumulative inner/decode points with `max_work_points`, caps the serialized bridge with response bytes, checks cancellation throughout, and reports `api_promql_intermediate_points` separately. The 512-series consuming shape materialized 16,384 points and raised whole-process HWM by a measured 2,988 KiB (8.96%) over the preceding identical-fixture run. | accepted |
| `QSF-030` | 2026-08-04 | `PQL-S09` | Adding recursive plan variants grew the by-value reader-channel command from a small control message to at least 240 bytes, tripping the strict production `large_enum_variant` gate. | Query grammar growth must not linearly inflate every queued reader command or reader-channel slot. | `reader_queue_commands_keep_recursive_query_plans_behind_indirection`; production all-target clippy gate | Box the owned `ReadRequest` once at the reader-queue boundary and keep retry/query semantics unchanged. The queue command is pinned at no more than 64 bytes; AST/storage contracts are unchanged. | resolved |
| `QSF-031` | 2026-08-04 | `PQL-O01` | Prometheus 3.13.2 removes `__name__` from a vector under unary minus, including after double negation; treating negation as a value-only map would retain a wrong metric identity. | Unary composition preserves non-name labels and timestamps but removes the metric name at every unary node. | pinned `PQL-O01-unary-vector-drops-name` API case; `session_four_promql_unary_minus_preserves_types_labels_limits_and_reopen` | Apply name removal in the Rust AST evaluator, keep the public extension/grid contract unchanged, and document SQL as the numeric storage foundation rather than a complete PromQL envelope. | resolved |
| `QSF-032` | 2026-08-04 | `PQL-O01` | A reusable Rust AST value bridge decodes and re-encodes child vectors, adding host work even though unary minus does not require another storage read. | Composition must stay bounded without moving language syntax into the extension or imposing owned-value allocation on streaming selector/window hot paths. | `docs/evidence/2026-08-04_session4_pql_o01.json`; intermediate counter and limit regression | Accept the measured bounded cost: versus the same-run 512-series × four-point selector grid, unary adds 1.272 ms p50 and 1.355 ms p95 while storage counters remain identical. The 35,536 KiB whole-process HWM is below Session 3's 36,332 KiB run. Reuse this bridge for binary AST nodes; no extension primitive is justified. | accepted |
| `QSF-033` | 2026-08-04 | `PQL-O02` | The root underscored-number compatibility shim captured `20 - metric_name` because the complete expression began numerically and contained an underscore later in the metric identifier. | Root-literal compatibility must never preempt a valid compound AST expression. | `root_underscored_number_shim_does_not_capture_binary_metric_names`; scalar-left real-extension arithmetic regression | Restrict the shim to input composed entirely of numeric-literal characters before validating underscores; compound input returns to the pinned PromQL parser. Malformed root underscore literals still fail. | resolved |
| `QSF-034` | 2026-08-04 | `PQL-O02`, `PQL-O05` | General vector/vector arithmetic must evaluate two independently selectable child expressions; the 512-series × four-point fixture therefore performs 1,024 candidate-chunk reads and decodes 32,768 stored points per query. | Keep each operand bounded and read exactly once, then match in Rust per evaluation timestamp without moving PromQL or private storage access into the extension. | `docs/evidence/2026-08-04_session4_pql_o02_o05.json`; real-extension cumulative-work/cardinality regressions | Accepted: 2,048 results measure 8.895/9.696/10.054 ms p50/p95/p99 with exactly 4,096 intermediate points. Identical-subtree CSE could help synthetic `metric + metric`, but is planner work rather than a storage primitive and needs a representative workload before adding cache complexity. | accepted |
| `QSF-035` | 2026-08-04 | `PQL-O09` | The Rust-only Prometheus exposition parser still rejected `NaN` and infinities solely because its predecessor had to materialize values through BEAM, even though the retained codec, named batch, engine, and packed raw frame preserve all IEEE bits. | A Rust/libSQL float-series path accepts valid Prometheus non-finite samples and preserves them through flush, packed query, aggregation, and reopen. | `prometheus_ingest_semantics`; `session_two_preserves_native_ingest_and_partial_success_contracts`; `session_five_promql_sum_groups_labels_limits_and_reopen`; `tests/cli.sh` section 18 | Removed the obsolete BEAM-era finite-only parser check. Ordinary SQLite row projection still represents NaN as SQL NULL; `TRF1` remains the public bit-exact query surface and the Rust API renders Prometheus `NaN`/`+Inf`/`-Inf` strings. The full ignored-contract gate caught and corrected two stale finite-only expectations. | resolved |
| `QSF-036` | 2026-08-04 | `PQL-O16` | Repeated queries against pinned Prometheus returned the same `count_values` groups in different JSON-array orders because ungrouped aggregation traversal order is not a language promise. The oracle harness incorrectly compared every vector positionally. | Oracle evidence compares complete label/sample identities without requiring order unless the operator explicitly promises it. | `test_promql_vector_comparison_distinguishes_promised_order`; pinned `PQL-O16-count-values-format` live-oracle case | Added explicit ordered/unordered fixture semantics. Unordered vectors are canonically compared without weakening label, timestamp, value, cardinality, or result-type checks; instant global `topk` remains positionally checked. No product or storage behavior changed. | resolved |
| `QSF-037` | 2026-08-04 | `PQL-R01` | The shipped whole-second `avg_over_time` fast path and its raw/subquery fallbacks all used naive summation. The cancellation fixture `[1e16, 1, -1e16]` returned `0` instead of Prometheus's `1/3`, two maximum finite samples overflowed before division, and the API's `TWB1` decoder rejected a valid NaN result even though the public frame preserves raw float bits. | Every direct, modified, subsecond, and subquery path applies the pinned compensated average with an overflow-safe incremental-mean fallback and accepts all valid IEEE window results. | `window_average_compensates_cancellation_and_falls_back_before_overflow`; `frame_decoders_reject_unknown_and_inconsistent_envelopes`; `session_six_promql_avg_over_time_is_compensated_ieee_bounded_and_reopenable`; `tests/cli.sh` section 45; pinned `PQL-R01-*` live-oracle cases | Corrected the general public `avg` window kernel so direct SQLite/libSQL users retain the fast path and exact finite/IEEE behavior. Corrected Rust raw/subquery composition and removed the stale server-only NaN rejection; no syntax, frame, storage, batching, or index contract changed. | resolved |
| `QSF-038` | 2026-08-04 | `PQL-R01` | The public packed raw waist exposed pruning/decode counters, but `timeless_window_batches` exposed only its result bytes. Direct SQL and Rust callers could not attribute candidate chunks, decoded input, buffered input, or returned grid points to a window query. | Every measured range reduction reports storage and result work without private-table inspection or server-only instrumentation. | `tests/cli.sh` section 22; `session_six_promql_avg_over_time_is_compensated_ieee_bounded_and_reopenable`; Session 6 evidence harness | Added per-process cumulative `window_batch_query_*` rows to the existing public `timeless_stats` surface and propagated them through metrics API stats. No storage, frame, transaction, or query-result behavior changed. | resolved |
| `QSF-039` | 2026-08-04 | `PQL-R02` | The public `min` window kernel used `f64::min`, so equal samples `+0,-0` returned `-0`; pinned Prometheus retains the first zero and returns `+0`. | Direct SQL, packed extension, raw fallback, and subquery reductions share ordered-comparison minimum semantics: later NaN is ignored, leading NaN is replaced by the first numeric sample, all-NaN remains NaN, and equal signed zeros retain the first sample. | `window_min_ignores_later_nan_replaces_leading_nan_and_preserves_first_zero`; `tests/cli.sh` section 45; `session_six_promql_min_over_time_boundaries_ieee_limits_and_reopen`; pinned `PQL-R02-*` live-oracle cases | Corrected the general public `min` window fold without changing its SQL signature, frames, storage formats, batching, indexes, or transaction behavior. Rust raw/subquery composition already used the oracle-compatible ordered comparison. | resolved |
| `QSF-040` | 2026-08-04 | `PQL-R03` | The public `max` window kernel used `f64::max`, whose signed-zero preference returned `+0` for the ordered samples `-0,+0`; pinned Prometheus retains the first sample and returns `-0`. | Direct SQL, packed extension, raw fallback, and subquery reductions share ordered-comparison maximum semantics, including NaN replacement/ignoring and first-sample signed-zero stability. | `window_max_ignores_later_nan_replaces_leading_nan_and_preserves_first_zero`; `tests/cli.sh` section 45; `session_six_promql_max_over_time_boundaries_ieee_limits_and_reopen`; pinned `PQL-R03-*` live-oracle cases | Corrected the general public `max` window fold without changing its SQL signature, frames, storage formats, batching, indexes, transactions, or migrations. | resolved |
| `QSF-041` | 2026-08-04 | `PQL-R04` | The public `sum` window kernel folded with naive `+`; `[1e16,1,-1e16]` returned `0`, while pinned Prometheus returns `1` through compensated addition. | Direct SQL, packed extension, raw fallback, and subquery reductions share cancellation-safe addition while preserving overflow, NaN, infinity, and zero behavior. | `window_sum_compensates_cancellation_and_preserves_ieee_results`; `tests/cli.sh` section 45; `session_six_promql_sum_over_time_is_compensated_ieee_bounded_and_reopenable`; pinned `PQL-R04-*` live-oracle cases | Corrected the general public `sum` window fold. No SQL signature, packed frame, storage format, batching, index, rollup, retention, transaction, or migration contract changed. | resolved |
| `QSF-042` | 2026-08-04 | `PQL-R06` | Pinned Prometheus preserves `__name__` for `last_over_time`, unlike the metric-name removal required by the neighboring `avg/min/max/sum/count_over_time` functions. | Parser/evaluator name policy is selected per range function rather than inferred from a generic reduction family. | `session_six_promql_last_over_time_preserves_name_ieee_limits_and_reopen`; pinned `PQL-R06-*` live-oracle cases | The Rust API retains the input name for direct and subquery last-value paths. Direct SQLite uses the existing `timeless_grid` surface; no extension or storage change was necessary. | resolved |
| `QSF-043` | 2026-08-04 | `PQL-R06` | The Rust packed-raw plan returns every bounded input before selecting last values: the measured wide request returns 16,384 raw points to Rust for 2,048 final grid points. The public row-oriented `timeless_grid` reduces in storage but would cross SQLite once per result row. | Add a new packed-grid primitive only if measured row/decode/copy cost is material and avoidable for direct SQLite/libSQL users. | `docs/evidence/2026-08-04_session6_pql_r06.json`; `SQL-PROM-025`; Session 6 evidence harness | Retain bounded packed-raw Rust composition: 512-series/four-step p95 is 3.499 ms with 37,012 KiB HWM. The existing direct-SQL grid is exact, so current evidence does not justify another extension vector. | accepted |
| `QSF-044` | 2026-08-04 | `PQL-R07` | The initial roadmap classified `first_over_time` as stable P0, but pinned Prometheus 3.13.2 rejects it unless the `promql-experimental-functions` feature is enabled. | Stable compatibility must not silently enable an upstream experimental language surface. | pinned `PQL-R07-experimental-disabled` API case; `session_six_promql_first_over_time_is_explicitly_experimental` | Reclassified the row as `experimental`/`EXP`. The stable Rust API emits an explicit compatibility error. Reconsider only with a separately configured experimental tier and an oracle process pinned with the feature enabled; existing public raw/SQL surfaces need no change. | resolved |
| `QSF-045` | 2026-08-04 | `PQL-O15`, `PQL-R09` | The shared Rust quantile helper used IEEE `total_cmp`, which reordered equal `+0`/`-0` values; pinned Prometheus keeps their input order and can therefore return a different signed-zero endpoint. | Cross-series and over-time quantiles share stable numeric ordering: raw NaN ranks low, numerically equal signed zeros retain input order, and interpolation preserves the oracle's resulting bits. | `prometheus_quantile_keeps_input_order_for_signed_zero_ties`; `session_five_promql_quantile_interpolates_per_step_and_reopens`; `session_six_promql_quantile_over_time_interpolates_ieee_and_reopens`; pinned `PQL-R09-quantile-over-time-*-zero-order` cases | Replaced `total_cmp` with a stable numeric comparator after explicit NaN handling and pinned both real-extension paths. No storage or frame contract changed. | resolved |
| `QSF-046` | 2026-08-04 | `PQL-R09` | The initial matrix listed `WINDOW`, but the public `pXX` kernel is fixed nearest-rank while PromQL `quantile_over_time` accepts a scalar expression and uses linear interpolation with distinct IEEE rules. | A nearby storage statistic is not relabeled as PromQL when its language semantics differ. | `SQL-PROM-027`; `session_six_promql_quantile_over_time_interpolates_ieee_and_reopens`; pinned `PQL-R09-*` live-oracle cases | Use bounded packed raw reads and Rust composition for exact semantics. Document ordinary SQL window ranking for finite direct-user workloads. No new extension primitive is justified. | accepted |
| `QSF-047` | 2026-08-04 | `PQL-R11` | Rust's default shortest float display emitted a 32-digit fixed decimal for the wide population variance. Pinned Prometheus switches finite vector/matrix sample strings to scientific notation below `1e-6` or at/above `1e21`, while its scalar JSON method deliberately retains fixed notation. | Vector/matrix samples use the oracle's exact `f`/`e` threshold, exponent sign/width, signed zero, and non-finite strings; scalar results retain the distinct fixed-number contract. | `prometheus_samples_match_the_pinned_json_float_thresholds`; `session_two_promql_scalar_literals_match_prometheus`; `session_six_promql_stdvar_over_time_is_population_ieee_and_reopenable`; pinned `PQL-S11-underscored-exponent-scalar` and `PQL-R11-stdvar-over-time-wide-magnitude` cases | Split scalar and sample response formatting exactly like the pinned Prometheus JSON paths. Storage bits and SQL/extension contracts were already correct and remain unchanged. | resolved |
| `QSF-048` | 2026-08-04 | `PQL-O14` | The earlier top/bottom-k regression accidentally assigned the first west-series samples to the second evaluation timestamp and later samples to the first timestamp, so a full parallel real-extension gate exposed unrelated values in the instant result. | The fixture must model the documented two-step series and make its instant and range expectations mutually consistent. | `session_five_promql_topk_bottomk_rank_per_step_and_reopen`; full ignored metrics storage-contract gate | Corrected only the fixture timestamps and retained all query expectations. Product, storage, parser, and evaluator behavior were already correct. | resolved |
| `QSF-049` | 2026-08-04 | `PQL-R12`, `PQL-S19` | Two earlier error-contract regressions used a syntactically valid `rate(range-vector)` query as their permanent unsupported-expression fixture, so implementing stable `rate` semantics correctly turned those requests into successful empty results. | A supported function over an empty selector succeeds with an empty result; permanent negative coverage must exercise invalid syntax or types rather than a roadmap feature. | `session_two_promql_errors_match_the_documented_contract`; `session_four_pins_promql_selector_window_errors_and_reopen`; `session_seven_promql_rate_extrapolates_resets_bounds_and_reopens` | Replaced the stale unsupported-function fixtures with the permanently invalid `rate(instant-vector)` type error while preserving exact `bad_data` and canonical-envelope assertions. No product, extension, or storage behavior changed. | resolved |
| `QSF-050` | 2026-08-04 | `PQL-R12`–`PQL-R14`, `PQL-H01`, `PQL-S23` | Pinned Prometheus 3.13.2 adds top-level `warnings`/`infos` annotations for non-counter metric names, invalid histogram quantiles, missing/malformed `le` labels, and material histogram monotonicity repair; the Rust API currently emits only `status` and `data`. | Numeric/result parity and optional diagnostic-envelope parity must be tracked separately rather than silently treating an extra oracle field as matched. | pinned live counter responses; exact `PQL-H01-invalid-quantiles`, `PQL-H01-material-decrease-forces-monotonicity`, and `PQL-H01-malformed-and-absent-bounds-are-skipped` annotation fixtures; Rust query-harness API comparator; metrics API compatibility documentation | Keep numerically exact rows shipped, document the current omission, and retain explicit P1 row `PQL-S23` for exact annotation text, source position, deduplication, and instant/range envelope placement. No storage or extension change is justified. | open |
| `QSF-051` | 2026-08-04 | `PQL-F01` | SQLite's built-in `abs(-0.0)` preserves the negative-zero bits, while Prometheus `abs` emits positive zero. | The executable SQL foundation and Rust API both match Prometheus for signed zero without inventing an extension primitive. | `tests/cli.sh` section 45; `session_eight_promql_abs_transforms_float_vectors_and_reopens`; pinned `PQL-F01-abs-finite-and-ieee-values` oracle case | The SQL recipe explicitly maps either zero to literal `0.0` before applying `abs`; the Rust evaluator uses IEEE `f64::abs` over the public packed frame. No storage, format, or extension behavior changed. | resolved |
| `QSF-052` | 2026-08-04 | `PQL-F06`, `PQL-F07` | The first 50-iteration wide `atan` evidence run had a 4.394 ms p50 but a noisier 6.399 ms p95 and 11.227 ms p99; the subsequent `sin` run measured 5.424/7.820/8.875 ms. Both retained the same candidate chunks, decoded samples, packed bytes, result cardinality, and allocation shape as neighboring one-vector transforms, while emitting roughly 98–100 KiB of nontrivial decimal results. | Preserve measured tails and distinguish host/runtime plus math/serialization cost from storage amplification; do not add an extension primitive without repeatable evidence of avoidable read, decode, allocation, copy, or row-crossing work. | Rust query-harness `inverse_wide` and `trig_wide` evidence cases; `docs/evidence/2026-08-04_session8_pql_f06.json`; `docs/evidence/2026-08-04_session8_pql_f07.json` | Accepted both honest runs. One public packed read still returns 2,048 results from 16,384 decoded points at bounded memory. The work counters rule out extra storage amplification; later comparable runs can separate scheduler variance from the expected transcendental-math and decimal-response cost. | accepted |
| `QSF-053` | 2026-08-04 | `PQL-F09` | Pinned Prometheus 3.13.2 accepts a nonempty UTF-8 destination label such as `bad-name` in `label_replace`, while an empty destination is an execution error; the initial matrix also claimed an ordinary SQL foundation even though SQLite has no portable RE2-compatible capture expansion. | Match the pinned Prometheus 3 label-name scheme and claim SQL only when a complete public parameterized statement exists. | pinned `PQL-F09-utf8-destination-label-name` and `PQL-F09-invalid-destination-label-errors`; `session_eight_promql_label_replace_pins_regex_labels_limits_and_reopen`; `docs/QUERIES.md` | The Rust API accepts every nonempty UTF-8 destination, rejects empty at execution, and owns full-match capture expansion. Changed the matrix foundation to `none`; no misleading SQL recipe or PromQL-specific extension primitive was added. | resolved |
| `QSF-054` | 2026-08-04 | `PQL-F09` | The fused Prometheus exposition parser deliberately retained `\n`, `\"`, and `\\` label-value escapes as raw bytes, so series identity and `label_replace` source values differed from Prometheus remote-write and text-format semantics. | Decode standard exposition escapes before resolving durable series identity without adding allocation to ordinary unescaped samples. | `prometheus_ingest_semantics`; `tests/cli.sh` section 18; `session_eight_promql_label_replace_pins_regex_labels_limits_and_reopen`; pinned `PQL-F09-dot-all-full-match` | Added an escape-only resolution path that decodes newline, quote, and backslash before the existing public cache/registry contract. The common borrowed path remains allocation-free; batching, compression, indexes, frames, transactions, migrations, and stored canonical label format are unchanged. | resolved |
| `QSF-055` | 2026-08-04 | `PQL-F09`, `PQL-F10` | Label replacement and joining initially relied on the final JSON encoder to enforce `max_response_bytes`, after all destination strings had already been allocated; repeated captures or source labels could therefore amplify intermediate memory before rejection. | Bound every appended replacement/join fragment and the cumulative generated destination-label bytes during evaluation, while retaining exact upstream replacement syntax and cancellation. | `session_eight_promql_label_replace_pins_regex_labels_limits_and_reopen`; `session_eight_promql_label_join_pins_sources_names_limits_and_reopen`; pinned `PQL-F09-replacement-dollar-and-missing-capture`; response-limit envelopes | Added an API-local incremental byte budget shared by both label functions. It fails with the existing response-size execution error before excess allocation, leaves the reader reusable, and changes no extension, SQL, frame, storage, transaction, or durability contract. | resolved |
| `QSF-056` | 2026-08-04 | `PQL-F16` | A selector's public HTTP sample timestamp is normalized to the outer evaluation grid, so implementing `timestamp()` as a generic post-response value transform would silently lose the selected stored sample timestamp. Pinned Prometheus distinguishes direct selector provenance from samples newly created by every composed expression node. | Direct selectors report the chosen stored sample time through lookback, `offset`, and `@`; unary, function, binary, aggregation, and range-function results report their evaluation time, while every response timestamp stays on the outer grid. | pinned `PQL-F16-timestamp-*` API cases; `session_eight_promql_time_timestamp_pin_clock_provenance_and_reopen`; `SQL-PROM-051`; Session 8 evidence | Added an API planner branch that reuses the existing public packed raw selector read and substitutes its decoded storage timestamp before envelope normalization. Composed expressions retain ordinary bounded evaluation. No extension primitive, private-table access, frame change, or second read was added. | resolved |
| `QSF-057` | 2026-08-04 | `PQL-F17` | Pinned Prometheus calendar functions truncate in-range fractional seconds toward zero but map NaN, both infinities, either finite overflow direction, and the exactly representable `i64::MIN` float to the `i64::MAX` Unix calendar; Rust's native saturating float cast would instead map negative overflow to `i64::MIN`, while SQLite date functions return NULL outside their smaller calendar range. | Calendar conversion must use an explicit checked float-to-second boundary in the Rust API; the direct SQL recipe must claim only the SQLite-representable finite subset and explicitly cast fractions before `strftime`. | pinned `PQL-F17-*conversion` and overflow API cases; `prometheus_calendar_components_cover_utc_and_non_finite_sentinel`; `session_eight_promql_calendar_part_one_uses_utc_defaults_and_reopens`; `SQL-PROM-052`; Session 8 evidence | Added one bounded API-local UTC calendar plan over the existing packed vector result, with an i128 civil-date calculation and explicit Prometheus overflow sentinel. The SQL foundation uses public grids plus ordinary SQLite functions. No extension primitive, extra read, private-table access, frame, storage, or transaction change was added. | resolved |
| `QSF-058` | 2026-08-04 | `PQL-H01` | Classic histogram families are ordinary float series, but correct grouping must retain the metric name as an internal discriminator before dropping it from output, and Prometheus ignores both upward and downward count deltas within a relative `1e-12` tolerance before forcing material decreases monotonic. A plain running SQL maximum loses the tiny-upward rule, while grouping only by visible output labels can merge distinct metric families. Pinned Prometheus then rejects two independently grouped families if name removal creates the same visible label set at one evaluation step. | Read each selected bucket once through the public bounded grid/frame, group and correct the decoded float vector in the Rust API, then apply the existing step-aware output collision normalizer; describe ordinary SQL only as the valid finite-data foundation. | pinned `PQL-H01-*` oracle cases including `PQL-H01-output-family-collision-errors`; `session_nine_promql_classic_histogram_quantile_matches_oracle_and_reopens`; `SQL-PROM-054`; Session 9 evidence | Added bounded API composition over existing public metric reads with strict Go-compatible bound parsing, equal-bound coalescing, exact correction/interpolation, internal name grouping, output collision rejection, cancellation, and cumulative work charging. No extension primitive, private-table access, storage format, batching, index, rollup, retention, transaction, or migration change is justified. | resolved |
| `QSF-059` | 2026-08-04 | `LQL-F05` | SQLite `json_extract` maps both an absent JSON path and a stored JSON null to SQL `NULL`; using it alone would collapse missing/null and could also compare typed projections without proving their JSON type. | Every arbitrary-field recipe and API predicate preserves JSON primitive type and distinguishes missing, null, and empty values over canonical rich metadata. | `SQL-LOG-005`; Rust SQL-equivalent semantic regression; `session_ten_typed_metadata_equality_distinguishes_nested_missing_null_empty_and_reopens` | Direct SQL pairs `json_type` with `json_extract`. The Rust API keeps legacy `field:value` string equality, accepts explicit JSON primitives through `field:=value`, uses indexed string columns only for candidate pruning, and performs the authoritative typed comparison after bounded decode. No new extension primitive or storage-format change is justified. | resolved |
| `QSF-060` | 2026-08-04 | `LQL-F08`, `LQL-F39` | A raw Rust `str::contains` phrase matched `failed`, attached prefixes, and underscore/Unicode continuations that pinned VictoriaLogs excludes; the existing extension `message_contains` is intentionally case-insensitive and therefore cannot be relabeled. The initial pipeline splitter also treated `|` inside a quoted phrase as syntax. VictoriaLogs permits byte escapes outside Timeless's retained UTF-8 rich-log/JSON domain. | Preserve exact quoted bytes, quoted pipeline characters, and Unicode word edges without weakening the retained storage type or claiming an inexact SQL/extension primitive. | pinned `tests/query_oracles/victorialogs/api_cases.json`; `session_ten_quoted_phrase_matches_victorialogs_case_and_bytes_and_reopens`; `session_ten_quoted_literals_and_field_identifiers_match_oracle_and_reopen` | Added quote-aware pipeline tokenization, bounded Rust phrase matching over public rows, complete applicable Go escape decoding, raw backticks, and literal quoted field names. Invalid UTF-8 byte escapes fail explicitly. Removed the misleading SQL-foundation claim; portable SQLite lacks the required Unicode-category boundary predicate. Storage, batching, block formats, indexes, transactions, and extension syntax remain unchanged. | resolved |
| `QSF-061` | 2026-08-04 | `LQL-Q07` | API row and response caps could reject only after a low-selectivity log scan had decoded whole candidate blocks; the first work guard also filtered/cloned the complete live buffer before rejecting its count. Dropping a timed-out HTTP future did not provide a cancellation checkpoint while Rust code was executing inside a virtual-table call. | A small output limit never hides unbounded buffer/filter/decode work, and timeout/client cancellation cooperatively stops both SQLite bytecode and extension block work without poisoning the reader. | `query_work_limits_bound_decode_and_cancellation_leaves_the_engine_reusable`; `session_ten_logsql_limits_cancel_errors_and_direct_sql_reuse_the_reader`; Rust SQL-equivalent harness | Added optional capability-advertised `max_work_entries` inputs to `timeless_logs`, `timeless_log_count`, and `timeless_log_values`; reject the protected live-buffer count before filtering/allocation, charge candidate entries before block decode, and check host progress/interrupt state before/after persisted reads. Old arities remain unbounded and compatible; the Rust server requires the capability and always binds its configured cap. No storage, block, batch, transaction, or durability format changed. | resolved |
| `QSF-062` | 2026-08-04 | `LQL-Q07` | The first cancellation implementation decremented `api_query_in_flight` when the HTTP future was dropped, even though its SQLite reader could still be unwinding cooperative cancellation. Moving ownership entirely to the reader then left the gauge stuck if cancellation happened while waiting to enter a full reader queue, before any reader owned the command. | In-flight telemetry remains nonzero until reader-side execution actually stops, while a request cancelled before queue admission retires immediately; cancellation is a separate monotonic counter. | `cancellation_accounting_transfers_only_after_reader_queue_admission`; `session_ten_logsql_limits_cancel_errors_and_direct_sql_reuse_the_reader` | Added an explicit ownership handoff after successful queue admission. A dropped waiter arms cancellation and increments `api_query_cancelled`; it decrements in-flight only before handoff, while reader completion owns the decrement afterward. The regressions pin both states, wait for actual zero, and prove the same reader can execute another extension query. | resolved |
| `QSF-063` | 2026-08-04 | `LQL-P09`, `LQL-Q08` | Pinned VictoriaLogs returns `stats count() as total` as a JSON string and collapses malformed and unsupported parser failures into HTTP 400 text. The established Timeless contract expects a numeric count and explicitly distinguishes unsupported capability from malformed syntax. | Select and document one stable product behavior per row instead of claiming wire identity or silently inheriting an upstream envelope that weakens Timeless capability errors. | pinned `tests/query_oracles/victorialogs/api_cases.json`; `session_ten_logsql_sort_offset_limit_count_survive_optimize_and_reopen`; `session_ten_logsql_limits_cancel_errors_and_direct_sql_reuse_the_reader` | Retained numeric JSON/SQLite INTEGER count. Timeless emits JSON HTTP 400 `invalid_query/malformed_logsql` and HTTP 422 `unsupported_capability/unsupported_logsql`; rejected syntax never reaches storage. Phrase, filter, ordering, and count cardinality still use the pinned upstream oracle. | accepted |
| `QSF-064` | 2026-08-04 | `LQL-Q07` | The Rust logs owner calculated block/entry/payload totals, optimizer source size, timestamp-unit compatibility, and index allocation by reading `logs_blocks`, `logs_meta`, and `logs_terms` directly. That coupled a public server to private extension layout and duplicated the optimizer's current source predicate. | Signal APIs and direct embedded hosts depend only on public virtual tables, scalar functions, capabilities, and commands; the extension remains the sole owner of shadow layout and maintenance policy. | Rust SQL-equivalent harness public-statistics regression; `http_uses_the_established_8192_entry_buffer_without_request_flushes`; `logs_server_private_shadow_table_access_fails`; public log-statistics documentation | Added `timestamp_unit`, persisted/live entry totals, raw/compressed block and payload totals, index allocation, and `optimize_source_*` rows to `timeless_stats('logs')`. The server consumes only those public rows plus whole-database SQLite PRAGMAs. A Rust contract gate rejects private log shadow-table names in server storage source. The additive statistics change does not alter formats, batching, indexes, compression, transactions, or maintenance behavior. | resolved |

Use stable IDs (`QSF-008`, `QSF-009`, ...). Do not delete resolved findings;
change their status and link the correcting regression.
